
  # Ledger Application Design

  This is a code bundle for Ledger Application Design. The original project is available at https://www.figma.com/design/h3PPnJFtezZ797Lzn08HSZ/Ledger-Application-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  